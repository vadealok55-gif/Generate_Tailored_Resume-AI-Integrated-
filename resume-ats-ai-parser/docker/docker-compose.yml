version: '3.8'

services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - DEVICE=cpu
    volumes:
      - ../data:/app/data
    restart: unless-stopped
